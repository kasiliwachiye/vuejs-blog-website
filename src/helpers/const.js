/*
    Export constants from here
*/

const baseUrl = "http://localhost:8080";
const requestHeaders = {
    Accept: "application/json",
    "Content-Type": "application/json",
};
const defaultType = "All types";

export { baseUrl, requestHeaders, defaultType };
